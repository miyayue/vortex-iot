# Frontend
O frontend é uma página de HTML puro com CSS e JavaScript simples.

O frontend faz requisições à API Rest do servidor por protocolo HTTP para receber e exibir os dados enviados pelo firmware.

